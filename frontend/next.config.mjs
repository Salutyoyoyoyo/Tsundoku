/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    compress: true,
    basePath: '',
    experimental: {
        appDir: true,
    },
};

export default nextConfig;
